# Base path Vite builds asset URLs against. Leave as "/" once a custom
# domain is attached to GitHub Pages for this repo. Set to
# "/maxie-apps-homepage/" (the repo name) if instead deploying to the
# default username.github.io/maxie-apps-homepage/ project Pages URL.
VITE_BASE_PATH=/
